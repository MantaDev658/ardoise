services:
  postgres:
    image: postgres:15-alpine
    container_name: ardoise-db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: ardoise
    ports:
      - "5432:5432"
    volumes:
      - ardoise-data:/var/lib/postgresql/data

volumes:
  ardoise-data: